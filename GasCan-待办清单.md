# 我的待办清单(2026-07-21)

> 个人跟进用,不影响 Kiro 的 spec 流程。标了「🔴 卡开发」的项,Kiro 那边会被卡住等你;标「🟢 不卡开发」的可以慢慢来,不耽误 Kiro 继续写代码。

## 🔴 卡开发 —— 优先处理

- [ ] **买域名 + 走 ICP 备案**
  这是目前整个项目最大的时间瓶颈,通常要 2~4 周,现在就该启动,不要等服务器批下来再弄。
  卡住的东西:微信网页授权回调域名必须是已备案域名(微信强制要求,新备案域名还要等 24 小时才能配置)→ 卡住「真实微信登录」的端到端联调;同时也卡「部署上线」,没有域名+HTTPS 服务没法给真实用户用。
  Kiro 现在能做的:代码可以先写(RealWechatAuthService 等),但没法真实跑通测试,得等这个。

- [ ] **腾讯云轻量服务器申请**(进行中)
  卡住:部署上线。没有服务器,写完的代码也没地方跑生产环境。
  Kiro 现在能做的:本地 docker-compose 环境不受影响,可以正常开发自测。

## 🟡 不卡开发,但会卡具体某个小功能,建议尽快做(不难,顺手就能弄完)

- [ ] **去服务号后台拿 AppID + AppSecret**(开发 → 基本配置)
  拿到后私信/邮件发给 Kiro,不要提交到 git 仓库里(填到 `.env` 的 `WECHAT_APPID` / `WECHAT_SECRET`,这个文件本来就在 `.gitignore` 里,不会传上去)。
  卡住:Kiro 写好 `RealWechatAuthService` 后,没有真实 AppID/Secret 就没法实际调用微信接口测试。

- [ ] **去服务号后台申请"催租提醒"模板消息模板,拿 template_id**
  卡住:`RealWechatNotifyService.sendTemplateMessage` 的具体模板消息发送,需要真实 template_id 才能填进代码。

## 🟢 完全不卡开发,可以慢慢来

- [ ] **补录租客真实姓名 / 手机号**(其余基础数据已从 Excel 导入)
  不影响 Kiro 继续开发,只影响正式上线后数据是否准确,上线前搞定就行。

- [ ] **把完整楼栋 Excel 发给我(Claude)**
  我来清洗成标准 CSV,配合 Kiro 已经写好的 `import:init` 导入命令用。不着急,Kiro 那边导入脚本已经完工,随时能接你的数据。

- [ ] **微信支付商户号申请**
  已经约定阶段二(P2)才做,现在完全不用管,收款码+人工确认的方案已经够用。

---

## 建议的处理顺序

1. 先启动域名购买 + ICP 备案(时间最长,越早越好)
2. 顺手把 AppID/Secret 和模板消息 template_id 这两个"举手之劳"搞定,发给 Kiro
3. 服务器申请继续走原有流程
4. 数据类的两项(补录租客信息、发 Excel 给我)有空就弄,不着急
5. 微信支付先不管
