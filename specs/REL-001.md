# REL-001 — Transaction Safety
Priority P1. Status LOCAL_VERIFIED / MYSQL_INTEGRATION_BLOCKED. Implementation base 68878b6 (includes SEC-001). Acceptance Commander. Coordinate leases.service.ts ownership with SEC-001.

## Scope
Review approveTerminationRequest / approveTransferRequest and nested endLease/create/bill/deposit/room/application writes. Choose explicit Prisma transaction context propagation or reliable persisted compensation; prefer a small atomic DB unit with external actions after commit. Include repair completion + maintenance cost linkage if the same demonstrated partial-write blocker exists, documenting evidence.

## Invariants
Termination approval atomically ends lease, changes room/deposit/bill and marks application resolved, or changes none. Transfer atomically ends old lease, creates intended new lease for the same tenant, updates room occupancy/financial records/application. Duplicate/concurrent approval must produce one result; competing attempts to reserve the same vacant room must not create overlapping active leases.
Provider/QR/message calls cannot be rolled back by Prisma. Do not hold a DB transaction open over network I/O. Persist enough existing workflow state for retry/reconciliation, and surface external failure without duplicating lease/deposit/bill/signature. No microservice/queue redesign.

## Acceptance
Inject failure at each intermediate DB step and assert all-or-nothing state on isolated MySQL. Concurrent approval/rejection, double approval and two applicants for one room tested. Retry after network timeout/failure must not recreate the financial operation; describe ambiguous provider outcome handling. Preserve existing amounts, refund rules, bill engine date semantics, historical data and manual confirmation behavior.
Backend typecheck/full Jest plus isolated DB integration evidence and affected browser flows. No production/dev DB mutation. If isolated MySQL unavailable, integration gate remains BLOCKED, not passed by mocks.

## Local implementation checkpoint — 2026-09-06 UTC

### Design and invariants
- `create`, `endLease`, termination approval/rejection and transfer approval/rejection use interactive Prisma transactions. Small internal helpers receive the transaction client, so nested lease, room, deposit, bill, bill-item, application and signing-task writes share one commit/rollback boundary without changing amounts, dates or refund rules.
- MySQL `SELECT ... FOR UPDATE` locks serialize the application row, lease row and target-room reservation. A repeated approval returns the already-approved result without another settlement; approval versus rejection observes one committed terminal state; competing transfers cannot both observe the same room as vacant after acquiring its row lock.
- Transfer approval creates the new lease and a local contract-signing task inside the database transaction, and persists an approval-supplied ID card when the existing tenant record lacks one. QR creation, PDF generation, WeiQian upload/task creation and customer messaging start only after commit. A post-commit retry reuses the persisted signing task instead of creating another lease, deposit entry or signing task.
- Repair completion and its positive-cost maintenance record use one transaction after locking the repair request. A failed cost-record write rolls back the resolved state, and concurrent completion cannot duplicate the maintenance cost.
- E-sign launch claims `FOLLOWED -> LAUNCHING` with a conditional update before provider I/O. PDF generation failure resets to `FOLLOWED` because no provider call began. Any upload/create error leaves the task in `LAUNCHING`: the remote outcome is ambiguous, so automatic retry is suppressed. The landlord lease detail labels this state `发起结果待核对` and directs the operator to search WeiQian by `LE-{taskId}` before recovery. After provider success, only the local persistence write is retried; final persistence failure logs the non-PII provider bId for manual recovery. `CREATED` is persisted before the supplementary customer-service message, and message failure is treated as best-effort rather than surfacing a false launch failure.

### Evidence on implementation worktree
- PASS — server typecheck: `apps/server/node_modules/.bin/tsc --noEmit`.
- PASS — full server Jest in the product timezone with local Playwright Chromium headless shell: run from `apps/server` as `TZ=Asia/Shanghai PDF_CHROME_EXECUTABLE_PATH=/root/.cache/ms-playwright/chromium_headless_shell-1187/chrome-linux/headless_shell ./node_modules/.bin/jest --runInBand`; independent review rerun passed 15 suites / 179 tests, including a real PDF render.
- PASS — focused lease and maintenance tests: 3 suites / 49 tests. Added injected bill-item/new-lease/repair-cost/QR/PDF/provider/customer-message/local-persistence failures, approval-versus-rejection, double approval, competing room applicants, concurrent repair completion and concurrent e-sign claims. These are unit/transaction-contract tests and do not prove MySQL rollback or lock behavior.
- PASS — landlord H5 typecheck and production build: `apps/landlord-h5/node_modules/.bin/vue-tsc -b` and `apps/landlord-h5/node_modules/.bin/vite build`.
- PASS — isolated Chromium frontend fixtures: `PLAYWRIGHT_CHROMIUM_EXECUTABLE=/root/.cache/ms-playwright/chromium_headless_shell-1187/chrome-linux/headless_shell node_modules/.bin/playwright test -c e2e/sec001.config.ts e2e/sec001/contracts.spec.ts`; 6 tests, including `LAUNCHING` visibility, reconciliation identifier and absence of a duplicate-launch action. Fixtures use synthetic API responses and do not prove database/provider behavior.
- PASS — `git diff --check`.

### Independent review — 2026-09-06 UTC
- FIXED — a provider launch that had already reached `CREATED` locally still threw when the supplementary WeChat customer message failed. That false failure could mislead a caller into thinking the launch itself failed. Messaging is now best-effort after the durable state transition, with a warning and a successful `CREATED` response.
- FIXED — after a successful WeiQian response, a transient local database failure previously received only one persistence attempt. The implementation now retries only the local write three times, never the provider call, and logs the non-PII provider bId if all local writes fail so a maintainer has a concrete recovery identifier.
- PASS — independent server typecheck; landlord and tenant typecheck/production build; 15 suites / 179 Jest tests; and six isolated Chromium contract cases.
- REVIEWED — approval, rejection, lease, room, deposit, bill, bill-item, request, signing-task and repair-cost writes use the interactive transaction client. QR, PDF/provider launch and messaging are outside the approval transaction. Database row locks and the conditional `FOLLOWED -> LAUNCHING` claim provide the intended serialization, subject to the real-MySQL blocker below.

### Remaining acceptance blockers
- BLOCKED — isolated MySQL integration. This execution environment has no Docker, `mysql`/`mysqld`, or isolated `DATABASE_URL`; no real database was mutated. Failure-at-each-write rollback, real lock waiting/deadlock behavior, concurrent approval/rejection, double approval and two applicants reserving one room must still run against an isolated MySQL instance.
- PENDING — real browser termination/transfer flows remain part of the isolated MySQL gate; the new `LAUNCHING` state itself passed fixture-browser interaction and layout checks.
- PENDING — provider-backed reconciliation evidence. The safe local behavior prevents an ambiguous timeout from duplicating a signature request, but an operator must verify the remote task before a maintainer resets or binds the local state. Do not mark REL-001 or the data-integrity release gate PASS until this recovery is rehearsed with an approved test identity/provider environment.

## Pending evidence checkpoint — real-MySQL integration harness added, not yet executed

- ADDED (test-only, no production/service/schema files touched) — `apps/server/src/testing/mysql-integration/` (guard, mutation-counting Prisma wrapper, synthetic fixtures, deterministic external-collaborator fakes, snapshot normalization, README) plus four suites: `apps/server/src/leases/__mysql-integration__/rel001-termination.mysql-integration.spec.ts`, `rel001-transfer.mysql-integration.spec.ts`, `rel001-concurrency.mysql-integration.spec.ts`, and `apps/server/src/maintenance/__mysql-integration__/rel001-repair-completion.mysql-integration.spec.ts`. A minimal `test:mysql-integration` script was added to `apps/server/package.json`.
- Design: real `Prisma.$transaction` interactive transactions against an isolated MySQL database, no mocked Prisma client. A test-only proxy awaits every successful mutation for real, counts it, and can throw immediately after a selected Nth mutation. Each suite first runs a discovery pass (no injected failure) to learn the real mutation count for the current code path, then injects failure at every one of those positions against a freshly recreated, collision-resistant synthetic fixture, comparing full persisted-row snapshots before/after to prove all-or-nothing rollback. Cleanup is narrow (only the fixture's own rows, FK-safe order, no truncate/global FK-disable/schema drop/broad delete) and only runs after a test's assertions pass, so a failing run's state is retained for inspection.
- Coverage implemented: termination approval within-deposit and shortfall paths (failure at every write, duplicate approval, concurrent approve-vs-reject on separate connections); transfer approval (failure at every write with assertions that old lease/room/tenant/request state is byte-for-byte unchanged and no new lease/room-status/signing-task artifact exists, duplicate approval, two competing applicants for one vacant room on separate connections); repair completion (failure before and after the positive-cost maintenance record write, duplicate concurrent completion on separate connections); and a dedicated concurrency suite proving real `FOR UPDATE` row-lock waiting time across two connections plus a deterministic, isolated two-connection MySQL deadlock with a bounded manual retry (test-only, no production change).
- Fail-closed guard (`guard.ts`): the ordinary full Jest run self-skips these opt-in suites, while the focused entry point fails nonzero unless the explicit opt-in is present; the URL targets exactly `127.0.0.1:33317/landlord_easy_e2e`; both the supplied candidate and worktree HEAD equal fixed SHA `104de1521cf194c9dc76ccca52741f05a75f1180`; and the independently inspected Docker project/container, loopback binding, accepted image digest, tmpfs, one network, zero volumes and empty application tables all match. No credential or full URL value is logged.
- NOT RUN — this checkpoint records implementation only. No command was executed, no Docker/MySQL instance was started, and no isolated database was mutated while writing this harness. The BLOCKED status above for isolated MySQL integration evidence remains BLOCKED until a human or CI actually runs `pnpm --filter server run test:mysql-integration` against a real, disposable `landlord_easy_e2e` instance with the guard env vars set and reports the exact candidate SHA, dated result, and reviewer.
- Known production-invariant risk to verify once run: Prisma's default interactive-transaction timeout (5s) and max-wait (2s) were assumed sufficient for the deadlock/lock-wait timing tests on the target environment; if the real MySQL instance is slow enough to exceed those, the concurrency suite's own transactions (not production code) may need a longer explicit `timeout` option. This does not affect any production file.

## Real-MySQL execution checkpoint — 2026-09-08 UTC — BLOCKED

- PASS — independent execution-lead and harness guards matched fixed candidate `104de1521cf194c9dc76ccca52741f05a75f1180`, loopback-only port 33317, database `landlord_easy_e2e`, accepted Docker project/service/image, healthy container, tmpfs storage, one project network, zero volumes, 22 application tables and a zero-row baseline. Missing opt-in was separately proven to fail the focused command nonzero.
- PARTIAL — focused Jest ran four suites / 11 tests. The transfer happy-path discovery plus rollback injection after every one of its eight successful transaction mutations passed, and duplicate transfer approval produced one new lease/signing task. The competing-applicant test then failed: both pending applicants committed approval against the same target room, producing two active leases for that room. This is a production data-integrity invariant failure, not a harness-only failure.
- STOPPED — after the invariant failure, no cleanup, retry, production edit, remaining focused test, full Jest, Copilot review, commit, push or PR creation was performed. The synthetic failure state remains in the isolated tmpfs database for Commander triage. Subsequent termination, repair and lock/deadlock suites refused to start because the harness correctly detected the retained non-empty database.
- PASS — server TypeScript typecheck completed before the runtime invariant failure. The harness itself remains test-only; no Prisma schema, production service/controller/DTO, frontend, lockfile or CI file changed.
- BLOCKER — REL-001 requires a separately authorized production fix preventing two concurrent transfers from committing active leases for one room. Current retained evidence has one overlapped room with two active leases and two approved transfer requests. Release data-integrity remains NO-GO.

## Production fix — 2026-09-09 UTC — stale-snapshot room reservation race

- DIAGNOSIS — the room-reservation step in `createInTransaction` (shared by direct lease creation and by transfer approval's new-lease creation) read the target room with a plain, non-locking `findUnique`, then later issued an unconditional `room.update` to `RENTED`. Under InnoDB REPEATABLE READ, a transaction's consistent (non-locking) reads all reuse the snapshot established by the *first* plain read of that transaction, which in the transfer path happens before the room's own `SELECT ... FOR UPDATE`. The `FOR UPDATE` lock correctly serializes write order, but the subsequent plain read of room status can still return the pre-existing snapshot rather than the other transaction's just-committed `RENTED` state, so both concurrent transactions can observe `VACANT` and both proceed to create an active lease for the same room. This matches the retained real-MySQL evidence (two approvals, two active leases, one room) and is consistent with the two-writes-13ms-apart timing.
- FIX (production, `apps/server/src/leases/leases.service.ts`) — replaced the read-then-write check in `createInTransaction` with a single atomic conditional claim: `room.updateMany({ where: { id, status: 'VACANT' }, data: { status: 'RENTED' } })`, requiring exactly one affected row before proceeding and throwing (404 if the room no longer exists, 400 otherwise) when zero rows are affected. `UPDATE ... WHERE` always operates on the latest committed row (a locking/current read), so it cannot observe a stale snapshot. The later unconditional `room.update({ data: { status: 'RENTED' } })` was removed; the claim now happens at that point instead. All downstream tenant/lease/deposit writes remain in the same interactive transaction, so a claim failure or any later failure rolls back the claim too.
- `approveTransferRequest` no longer performs its own separate, pre-claim status check (which was equally vulnerable to the stale-snapshot read and, in the same transaction, would have double-claimed the room). It still does a plain existence lookup for the target room's `roomNo` (used in the old lease's end reason) and defers the VACANT decision entirely to `createInTransaction`'s atomic claim, passing through the pre-existing "目标房间不存在" / "目标房间不是空置状态" messages so external behavior is unchanged. Direct lease creation (`create()`) shares the same fixed code path and default messages; no separate change was needed there since it already funnels through `createInTransaction`.
- Preserved: all amounts, dates, deposit/refund math, idempotency (duplicate/rejected/re-approval handling unchanged), signing-task creation, and post-commit QR/provider/messaging behavior. No Prisma schema, migration, controller, DTO, frontend, lockfile or CI file changed.
- TESTS — added focused unit coverage in `leases-requests.service.spec.ts` for the new atomic claim: successful claim on direct `create()`, failed claim (count 0) rejecting without creating tenant/lease/deposit records, and claim-failure-with-room-missing returning 404; updated the existing transfer-approval unit tests (non-vacant rejection, success path, id-card backfill, post-commit QR failure, new-lease-write failure, and the two-competing-applicants case) to mock `room.updateMany` instead of the removed unconditional `room.update`. The retained real-MySQL suite `rel001-transfer.mysql-integration.spec.ts` (competing applicants) was not modified — its assertions (`BadRequestException`, one winner, one loser, target room `RENTED`) already describe the fixed invariant; it still requires an isolated MySQL run to move this checkpoint from fix-implemented to fix-verified.
- NOT RUN — no MySQL integration command was executed as part of this fix (no isolated MySQL instance available in this session). Server unit/typecheck were not executed in this session either; a maintainer/CI must run `apps/server` typecheck, full Jest, and `test:mysql-integration` (including the competing-applicants case) before this checkpoint can be marked verified and the release gate reconsidered.
