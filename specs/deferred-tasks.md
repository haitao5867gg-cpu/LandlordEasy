# 夜间值班延期任务队列（Night-Deferrd Queue）

> 用途:夜间值班会话(能力受限模式)遇到**超出其权限/能力**的任务时,把任务完整记录到这里,**不要尝试执行**。
> 处理人:白天主会话(GLM-5.3)读本文件,逐条处理并勾选。
> 记录格式:照着下面的模板追加,越具体越好(附GasCan原话、涉及文件、为什么没做)。

## 待处理队列

## [2026-09-20 23:28] 合同模板及全系统清理燃气相关内容(公寓无燃气服务)
- GasCan原话:"现有的合同模版要调整,我们的公寓楼不允许用燃气,看到所有跟燃气有关的地方都要调整。我们也不提供燃气服务。"
- 要做什么:①合同PDF模板(apps/server/src/contract-pdf/contract-pdf.template.ts)里燃气相关条款/字段删掉或改文案;②发起签署弹窗的"燃气表底数"字段(LeaseDetail.vue contractForm.gasMeterReading,前端可顺手处理但后端模板渲染要同步);③排查水电燃气费用项、交接清单默认值等所有燃气痕迹(grep 燃气/燃气表/gas)
- 为什么夜间没做:合同模板是 apps/server 后端代码,牵扯已上线dev的电子签PDF渲染,改错影响 GasCan 真人签署验证
- 建议处理方式:主会话改模板时同步删前端 gasMeterReading 字段,保证两端一致;改完重新生成一份PDF核对页数(固定8页约束不能破)
- 状态:待处理

## [2026-09-20 23:28] 水电表底数机制与实际运营不符,需重新设计口径
- GasCan原话:"关于水电表底数这个功能,我这里每个新客住进来,水电表都会清零。前一个租客走的时候会结算剩余水电费。我们的水费电费是即充即用的形式。"
- 要做什么:重新梳理"表底数"业务口径——实际运营是每任租客表读数从0开始(前客走时已结算),系统里"入住表底数"概念应该改为0或整体删除;涉及发起签署表单(waterMeterReading/electricityMeterReading)、合同PDF模板、可能的账单水电计算逻辑(BillEngineService)
- 为什么夜间没做:横跨后端模板+账单引擎,且"即充即用"下水电费是否还走系统账单、怎么生成账单需要先跟GasCan确认业务口径,不能猜
- 建议处理方式:主会话先跟GasCan确认3个问题:①表底数字段是删除还是固定填0 ②水电费账单还生成吗(即充即用=租客直接充水电,系统只记账不代收?) ③退租结算水电的流程要不要在系统里留痕
- 状态:待处理

## [2026-09-20 23:45] 微签签署页"签署说明"空白框——先确认API是否支持,支持则填入引导文案
- GasCan原话:"我通过链接打开了微签的签署界面……里面还有签署说明,是空白的,你看看相关的接口是否可以支持在这边添加一些文案,如果可以的话我们可以想下这里放点什么比较好"
- 查证结果(夜间会话已完成):微签API文档存档(docs/微签API文档.md v1.0.0)中`eachSign/create`的全部核心参数(launchAccount/fBIds/rType/authType/isSendSmsToReceiver/expiresTime/finishSignJumpPage+parm/receiverDTOS/positionDTOS/launcherSignRule)**没有签署说明类字段**;我们代码里也只传了文档内字段(apps/server/src/weiqian/real-weiqian.service.ts)。要么微签有未写入文档的可选字段,要么该框需在微签PC管理后台配置
- 要做什么:①把这个问题加进微签技术支持问题清单(GasCan或主会话转达):"签署页'签署说明'空白框,API能否传文案?对应哪个参数?"②若支持→改CreateEachSignTaskParams+real-weiqian.service+leases.service调用处(后端);若只能后台配→GasCan在微签后台配置
- 建议文案(GasCan挑选/修改):"请先点【预览】核对全部8页合同,确认租金/租期/押金无误后再点【下一步】完成实名认证和手写签名。签署完成后合同自动回传,可在租客端'我的租约'下载。"
- 为什么夜间没做:改apps/server后端代码=红线;且字段是否存在需微签技术支持确认,不能盲传参数
- 状态:待处理(等微签技术支持答复)

## [2026-09-20 23:45] 车牌"拍照识别"功能(OCR)
- GasCan原话:"我在别人的网上都是可以滚动选择拍照"(结合上下文:车牌输入希望支持拍照识别)
- 要做什么:车牌键盘已上线(commit 5f1ed73,固定位数+分段键盘),"拍照识别"需OCR能力(第三方车牌识别API或微信OCR接口),识别结果回填表单
- 为什么夜间没做:需新增后端接口或前端直连OCR服务+可能产生费用,超出绿灯区
- 建议处理方式:主会话评估方案(微信插件/第三方OCR/微签无关),跟GasCan确认是否真的需要(键盘输入已很快,拍照属锦上添花)
- 状态:待处理

## [2026-09-20 23:28] 水电单价做成全局配置(现价:水8元/吨,电1.3元/度)
- GasCan原话:"公寓统一都是水费8元一吨,电费1.3元一度电。这个费用面向所有租客一样的,建议做成全局配置。存在未来可能调整价格的情况"
- 要做什么:系统设置加"水电单价"配置项(参照合同签约设置ContractSettings的全局配置模式),水电费计算处读配置,设置页可改;当前默认值水8元/吨、电1.3元/度
- 为什么夜间没做:需要数据库schema变更(配置表加字段)+后端账单计算逻辑改动,均为红灯区
- 建议处理方式:跟"水电表底数机制"条目一起设计,都在水电费这条链路上,一次改完少动两次库
- 状态:待处理

## 记录模板

```
## [日期 时间] 简短标题
- GasCan原话:"..."
- 要做什么:(具体描述)
- 为什么夜间没做:(超权限:后端改动/数据库/生产/真实额度/复杂排查,或能力不足)
- 建议处理方式:(涉及哪些文件、注意什么)
- 状态:待处理
```

## 夜间会话的红线(只记录,不执行)

1. 一切生产环境操作(landlordeasy.cn、/opt/landlord-easy、main分支、deploy.sh prod)
2. 后端代码改动(apps/server)与数据库schema变更(prisma db push)
3. 微信/微签真实接口调用(消耗额度)、.env任何修改、WECHAT_MODE等模式切换
4. 复杂多步排查/架构改动/依赖升级
