# 微签技术支持问题清单(请GasCan转达,2026-09-21整理)

1. **【2026-09-21 21:56 生产实测,优先级最高】接收方打开短链登录后页面显示
   `{"code":"TokenNotFound","msg":"Token not found.","timestamp":1789991802611}`**
   - 场景:正式环境互签任务 shortCode=20-QZw(id=102172,2026-09-21 19:55:35发起,接收方
     18918690456)。签署人2026-09-19曾在测试链路上登录过微签H5(同一部手机、同一微信)。
     19:56:42在微信里点开 `/q/20-QZw` → 互签登陆页走短信验证码登录 → 随后页面变成上面的裸JSON。
   - 我方排查(全部用curl独立复现过):`/q/{shortCode}`短链本身健康(302→each_login正常,
     `no/eachSign/query`返回任务数据完整);`TokenNotFound`格式与贵方需要登录态的接口
     (如 `eachSign/queryAtta?receiveId=103847` 不带Authorization时)的响应逐字一致。
   - 疑问:①这是不是"手机上残留了旧的微签H5登录态(localStorage/cookie),导致新任务登录
     流程拿到无效token"?②如果是,老用户第二次签合同都会遇到,有没有绕开或清理办法?
     ③接收方登录成功后返回的token有效期是多久?
2. 接收方签署页的"签署说明"空白框,API能否传文案?对应eachSign/create的哪个参数?
   (文档v1.0.0的全部核心参数里没有签署说明类字段;若只支持后台配置请告知路径)
3. 接收方签署页能否隐藏与本任务无关的签署方式按钮(盖章/时间戳/批注/上传图片)?
   我们已用positionDTOS.sealType=10强制指定位置手写,但页面上仍显示多余按钮。
4. 正式环境的接收人签署页域名前缀是什么?(测试环境是http://forwave.picp.net:8888/q/{shortCode},
   上线前必须换成正式环境地址,WEIQIAN_SIGN_BASE_URL要配它)
   (补充:我们已确认 https://www.weiqian.com.cn:8887/q/{shortCode} 在正式环境可用,此问转为
   求官方确认这就是正式环境标准前缀)
5. (顺带)positionDTOS坐标精度:X/Y是0-1000千分比已确认;x从左、y从底部起算——我们的理解对吗?
   (已用真实文件像素校准过,此问仅求官方确认,避免正式环境表现不同)
