# 微签技术支持问题清单(请GasCan转达,2026-09-21整理)

1. 接收方签署页的"签署说明"空白框,API能否传文案?对应eachSign/create的哪个参数?
   (文档v1.0.0的全部核心参数里没有签署说明类字段;若只支持后台配置请告知路径)
2. 接收方签署页能否隐藏与本任务无关的签署方式按钮(盖章/时间戳/批注/上传图片)?
   我们已用positionDTOS.sealType=10强制指定位置手写,但页面上仍显示多余按钮。
3. 正式环境的接收人签署页域名前缀是什么?(测试环境是http://forwave.picp.net:8888/q/{shortCode},
   上线前必须换成正式环境地址,WEIQIAN_SIGN_BASE_URL要配它)
4. (顺带)positionDTOS坐标精度:X/Y是0-1000千分比已确认;x从左、y从底部起算——我们的理解对吗?
   (已用真实文件像素校准过,此问仅求官方确认,避免正式环境表现不同)
