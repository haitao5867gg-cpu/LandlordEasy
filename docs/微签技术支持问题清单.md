# 微签技术支持问题清单(请GasCan转达,2026-09-21整理)

> 2026-09-21 21:40 更新:原第1条"TokenNotFound 裸JSON"已查明是**我方环境变量配置错误**
> (WEIQIAN_SIGN_BASE_URL 多拼了一个 /q 路径,生成 /q/q/{shortCode} 链接,贵方网关对该
> 路径直接返回JSON),已在系统侧修复,**不再需要贵方排查**,该条撤回。以下为仍需贵方答复的问题。

1. 接收方签署页的"签署说明"空白框,API能否传文案?对应eachSign/create的哪个参数?
   (文档v1.0.0的全部核心参数里没有签署说明类字段;若只支持后台配置请告知路径)
2. 接收方签署页能否隐藏与本任务无关的签署方式按钮(盖章/时间戳/批注/上传图片)?
   我们已用positionDTOS.sealType=10强制指定位置手写,但页面上仍显示多余按钮。
3. (建议反馈,非提问)短链路径不合法时(如 /q/q/xxx),贵方网关会直接把
   `{"code":"TokenNotFound"...}` 等JSON当页面返回给签署人,普通用户看不出是什么错误。
   建议对非法短链返回友好提示页。我们已在系统侧保证链接正确,此条仅供贵方参考。
4. 求官方确认:正式环境接收人签署页前缀就是 `https://www.weiqian.com.cn:8887/q/{shortCode}`
   对吗?(已实测可用,只求确认没有其他标准域名)
5. (顺带)positionDTOS坐标精度:X/Y是0-1000千分比已确认;x从左、y从底部起算——我们的理解对吗?
   (已用真实文件像素校准过,此问仅求官方确认,避免正式环境表现不同)
