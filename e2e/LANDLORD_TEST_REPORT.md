# 房东端测试报告

## 执行信息

- 执行时间：2026-08-24
- 目标环境：`https://dev.landlordeasy.cn`
- 测试账号：`mock_landlord_001`（张大海）
- 浏览器：系统 Chrome（Playwright `channel: 'chrome'`）
- 执行命令：`pnpm exec playwright test --config=e2e/dev-remote.config.ts e2e/tests/landlord-login-home-rooms.spec.ts`
- 最终统计：**16 passed，0 failed，耗时 53.1s**

## 2.1 登录

| 用例 | 结果 |
|---|---|
| 2.1.1 mock 模式登录 | ✅通过：访问 `/login?mock_openid=mock_landlord_001` 后登录接口成功，自动跳转 `/`，工作台正常显示。 |
| 2.1.2 OpenID 留空 | ✅通过：清空输入框后点击登录，toast 显示“请输入 OpenID”；点击前后登录请求计数不变，未新增请求。 |
| 2.1.3 不存在/未启用 openid | ✅通过：使用 `E2E_QA_` 前缀的不存在 openid 请求返回 401，页面显示“无权访问:当前微信未加入房东白名单”；文案为中文，未出现异常类名、堆栈或 Prisma 等技术信息。 |
| 2.1.4 已登录访问保护页 | ✅通过：已有登录态时直接访问 `/rooms`，停留在房间管理页，未被重定向到登录页。 |
| 2.1.5 无效 token | ✅通过：携带无效 token 访问 `/rooms` 后自动跳转 `/login`，toast 显示“登录已过期,请重新登录”，本地 token 被清除。 |

## 2.2 工作台

| 用例 | 结果 |
|---|---|
| 2.2.1 四张卡片正常加载 | ✅通过：空置房间、30天内到期、逾期账单、待确认收款共 4 张卡片均显示，逐项与 `/dashboard/vacancy`、`/dashboard/expiring`、`/dashboard/overdue`、`/payments/pending` 的当次响应一致。 |
| 2.2.2 卡片跳转 | ✅通过：4 张卡片依次跳转到 `/dashboard/vacancy`、`/dashboard/expiring`、`/dashboard/overdue`、`/payments/pending`。 |
| 2.2.3 overdue 接口局部失败 | ⚠️体验问题：Playwright 将 `/dashboard/overdue` 返回设为 500；其余成功接口当次分别返回空置房间 `13`、30天内到期 `137`、待确认收款 `0`，页面最终显示的四张卡片数值均为 `0`（空置 `0`、到期 `0`、逾期 `0`、待确认 `0`）。仅记录实测现象，不作缺陷结论。 |
| 2.2.4 底部 tabbar | ✅通过：工作台、房间、账单、我的依次跳转到 `/`、`/rooms`、`/bills`、`/mine`，每次当前入口均带 `van-tabbar-item--active` 高亮。 |

## 2.3 房间管理

| 用例 | 结果 |
|---|---|
| 2.3.1 楼栋与状态组合筛选 | ✅通过：页面显示全部、Q栋、R栋、S栋、明远公寓楼栋 Tab；抽查“Q栋+空置”“R栋+已租”“S栋+维修中”三组，接口结果中的楼栋 ID/状态均符合筛选条件，UI 条数与状态标签同步一致。 |
| 2.3.2 空筛选结果 | ✅通过：动态找到无数据的楼栋+状态组合并通过 UI 切换，页面显示“暂无房间”，未白屏或报错。 |
| 2.3.3 返回列表保留筛选 | ✅通过：选择 R栋+空置并进入 314 房间详情，点击返回后仍保持 R栋和空置 Tab 高亮，314 仍在筛选列表中。 |
| 2.3.4 批量建房 UI 回归 | ✅通过：楼栋和房型底部选择器可选择并回填；最终执行中在 Q栋单间创建 903 成功；再次创建 903 返回“均已存在”清晰提示；创建 903–904 时成功创建 904，并提示 903 已存在未创建。此前调试执行还创建了 Q栋 900–902，详见残留数据。 |
| 2.3.5 新签/退租后返回列表状态 | ⚠️体验问题：Q栋 903 初始显示“空置”；新签成功后不刷新、直接返回列表仍显示“空置”；刷新列表后显示“已租”；进入详情完成退租后不刷新、直接返回列表仍显示“已租”。仅记录实测现象，不作缺陷结论。 |
| 2.3.6 当前欠费显示 | ✅通过：从真实账单数据选取一个在租且有待付/逾期账单的房间，详情页“当前欠费”与该房间所有 ACTIVE 租约下待付/逾期账单金额汇总一致；另选在租且账单全部已付房间，详情页不显示“当前欠费”。 |
| 2.3.7 详情四个 Tab | ✅通过：使用 Playwright route 提供带 `E2E_QA_` 标识、仅存在于浏览器内的有数据/无数据响应，逐一验证租约、维修记录、支出、操作日志四个 Tab；有数据时显示记录，无数据时分别显示“暂无租约”“暂无记录”“暂无支出”“暂无日志”。该用例未向 dev 数据库写入数据。 |

## 测试数据与清理

- 未新建楼栋或房型。
- 残留房间：`Q栋 900`、`Q栋 901`、`Q栋 902`、`Q栋 903`、`Q栋 904`。这些均为本批批量建房回归产生的 9xx 高位房号；当前房间页面没有删除入口，无法按“仅通过 UI 清理”的约束删除。
- `Q栋 903` 关联一个名称带 `E2E_QA_` 时间戳前缀的测试租客和一份测试租约；测试已通过 UI 完成退租，租约状态为已结束，房间后端状态已回到空置。租客和历史租约没有 UI 删除入口，仍会保留。
- 2.3.7 的 `990001`/`990002` 房号及 `E2E_QA_` 展示记录均为 Playwright route mock，仅存在于浏览器响应中，不是数据库残留。
- 本次未使用数据库脚本、Prisma 或直接数据库操作进行清理。

- 本批（2.4–2.7）为避免触碰历史租约，所有写操作均使用本次新建、备注或租客姓名带 `E2E_QA_` 前缀的 Q栋高位房号。调试失败触发 Playwright worker 重启并重复执行前置造数；实测残留 15 个本批前缀：`E2E_QA_66034558`、`E2E_QA_66054555`、`E2E_QA_66071494`、`E2E_QA_66176767`、`E2E_QA_66341554`、`E2E_QA_66460870`、`E2E_QA_66669953`、`E2E_QA_66864617`、`E2E_QA_67014930`、`E2E_QA_67163280`、`E2E_QA_67259594`、`E2E_QA_67476914`、`E2E_QA_67582930`、`E2E_QA_67751602`、`E2E_QA_67837931`。
- 本批残留房间共 285 间（每个前缀 19 间），位于 Q栋 `700–887`（不含既有 888）、`889–899`、`905–990`（其中部分号段已有前批/既有房间，实际本批房号已通过房间 `remark` 前缀逐条核对）。最终成功执行使用前缀 `E2E_QA_67837931`，对应 19 间房。
- 本批残留 255 份测试租约（232 份 ACTIVE、23 份 ENDED），租客姓名均带上述 `E2E_QA_` 前缀；残留 237 张账单（210 张 PENDING、27 张 PAID、0 张 OVERDUE）和 72 条收款记录（31 条 CONFIRMED、37 条 PENDING_CONFIRM、4 条 REJECTED）。这些统计通过 dev API 只读核对。
- 2.6.3、2.6.6 使用本批自建账单并由 Playwright route 在浏览器内模拟 OVERDUE；2.6.6 的模拟滞纳金没有写入数据库。2.7.4 的空列表同样仅为浏览器 route mock。
- 本批未修改或记账任何真实房东历史租约/账单；未执行数据库脚本或直接数据库清理。房间、租约、账单在当前前端没有可用删除入口，因此以上数据保留待统一清理。测试文件现已配置根级串行模式，避免后续单条失败因 worker 重启再次重复造数。

### 批次三（2.8–2.11）

- 本批统一使用前缀 `E2E_QA_1787576980659`；测试通过单 worker、零重试、串行套件执行，并能在一小时内的重跑中识别最近批次、按内容/备注精确复用已有记录。调试及最终全量重跑没有重复新增维修或支出记录。
- 维修记录残留 **2 条**：ID `1` 为房间 ID `301`、内容 `E2E_QA_1787576980659_正常维修`、费用 `91.25`；ID `2` 为 2.9.2“内容留空”提交后产生，房间 ID `301`、内容为空、费用 `92.03`、日期 `2026-08-24`。第二条因用例要求内容留空，无法在内容字段携带前缀，后续清理可用 `id=2`，或按房间 `301` + 日期 `2026-08-24` + 费用 `92.03` 精确定位。
- 支出记录残留 **3 条**（ID `1322`–`1324`）：正常支出 1 条、类目留空后产生 1 条、名称留空后产生 1 条；三条 `remark` 均带 `E2E_QA_1787576980659` 前缀，可统一检索清理。
- 2.9.3 使用不存在的房间 ID `999999999` 提交后未产生维修记录。2.8.4 的空看板及点击验证使用真实页面点击和 Playwright 空响应模拟，未写数据库；报表用例均为只读。
- 本批未新建房间、租约、账单或收款记录，未修改真实历史业务记录，也未使用数据库脚本、Prisma 或直接数据库操作清理。

## 本批执行信息（2.4–2.7）

- 执行时间：2026-08-24
- 执行命令：`pnpm exec playwright test --config=e2e/dev-remote.config.ts e2e/tests/landlord-lease-bill-payment.spec.ts`
- 最终统计：**27 passed，0 failed，耗时 2.1m**

## 2.4 新签租约

| 用例 | 结果 |
|---|---|
| 2.4.1 正常签约全流程 | ✅通过：在本批新建空置房间填写租客、预设 6 个月租期、租金和押金后签约成功；邀请码符合 8 位大写字母数字格式，成功弹窗正确展示，点击复制后 toast 显示“已复制”且剪贴板内容一致。 |
| 2.4.2 必填项留空 | ✅通过：姓名、手机号、起租日分别留空提交时，前端依次显示“请填写姓名”“请填写手机号”“请选择起租日”，均未发送签约请求；租期进入页面即默认选中“1年”，再次点击仍保持选中，页面没有可将租期取消为空的操作路径。 |
| 2.4.3 月末到期日计算 | ✅通过：起租日设为 `2027-01-31`、租期 1 个月后，页面自动显示到期日 `2027-02-27`（先将目标月夹到 2 月末，再按“到期日为结束日前一天”计算）。 |
| 2.4.4 附加费用项 | ✅通过：使用自定义 2 个月租期，添加完整费用 `E2E_QA_*_物业费/88` 和仅有名称的不完整费用；请求及租约详情均只保存完整的 1 条，不完整项被静默过滤，提交未报错。 |
| 2.4.5 已租房间重复签约 | ✅通过：对本批预置为已租的测试房间再次提交签约，后端返回 400，页面提示“房间不是空置状态,无法签约”。 |
| 2.4.6 手机号复用行为 | ⚠️现象记录：使用既有测试手机号、不同的 `E2E_QA_` 姓名签约成功；新租约 tenantId 与原租客相同（最终执行均为 tenantId `946`），租约详情显示原租客姓名 `E2E_QA_67837931_reuseAnchor`，未采用本次提交的新姓名。仅记录实测现象。 |
| 2.4.7 签约房间信息 | ⚠️体验问题：新签租约页标题和表单中均未显示当前签约房间的楼栋或房号；房间 ID 仅存在于 URL query 中。 |

## 2.5 租约详情

| 用例 | 结果 |
|---|---|
| 2.5.1 ACTIVE 租约详情 | ✅通过：租客、手机、房间、租期、月租金、押金、状态、邀请码均完整展示；状态显示“在租”，本批账单列表金额及待付状态 Tag 与接口一致。 |
| 2.5.2 合法续签 | ✅通过：新到期日填写未来日期并将新租金留空后续签成功，toast 显示“续签成功”，详情页到期日刷新，接口复核租金保持原值。 |
| 2.5.3 续签日期边界 | ⚠️现象记录：原起租日 `2026-08-24`，提交早于起租日和原到期日的新到期日 `2026-07-25`；请求返回 201/success，详情及接口中的到期日更新为 `2026-07-25`。仅记录实测现象，不作结论。 |
| 2.5.4 正常退租 | ✅通过：填写当日退租、退还押金 200 和 `E2E_QA_` 扣款原因后提交成功；接口复核租约状态为 ENDED，关联测试房间状态为 VACANT。 |
| 2.5.5 超额退还押金边界 | ⚠️现象记录：租约实际押金 500，提交退还 1499；请求已发送，随后接口复核租约状态变为 ENDED。仅记录实测现象，不作结论。 |
| 2.5.6 已结束租约按钮 | ✅通过：预置 ENDED 租约详情显示“已退租”，页面不显示“续签”和“退租”按钮。 |
| 2.5.7 交接记录入口 | ⚠️功能现状：ACTIVE 租约详情中未找到“交接记录”或“确认交接”入口；本用例只确认前端现状，未测试交接记录接口。 |

## 2.6 账单

| 用例 | 结果 |
|---|---|
| 2.6.1 四个状态 Tab | ✅通过：全部、待付、已付、逾期依次切换并等待真实接口完成；各筛选接口返回项状态均符合参数，UI 条数及状态 Tag 与接口结果一致，空结果显示“暂无账单”。 |
| 2.6.2 PENDING 操作按钮 | ✅通过：本批 PENDING 账单详情仅显示“追加费用项”和“手动记账(现金/转账)”，不显示“追加滞纳金”。 |
| 2.6.3 OVERDUE 操作按钮 | ✅通过：基于本批自建账单，用 Playwright route 将详情响应状态模拟为 OVERDUE 后，页面同时显示“追加费用项”“追加滞纳金”“手动记账(现金/转账)”三个按钮；未读取或修改历史逾期账单。 |
| 2.6.4 PAID 操作按钮 | ✅通过：本批预置全额记账后的 PAID 账单显示“已付”，三个操作按钮均不显示。 |
| 2.6.5 追加费用项 | ✅通过：对本批账单追加 `E2E_QA_*_临时费用/66`，页面费用明细出现该项，接口复核总金额准确增加 66。 |
| 2.6.6 追加滞纳金 | ✅通过：使用本批自建账单并在浏览器内模拟 OVERDUE；点击追加后模拟返回的滞纳金为 508，与 RENT 项 508 相等，总额由 508 增至 1016，明细和总金额同步刷新。该用例未写入真实滞纳金数据。 |
| 2.6.7 手动部分支付 | ✅通过：对本批待付账单手动记账 170（小于总额），支付记录出现“现金 ¥170”，接口复核账单仍为 PENDING，新增记录状态为 CONFIRMED。 |
| 2.6.8 手动全额支付 | ✅通过：对本批待付账单按总额手动记账后，详情状态自动刷新为“已付”，接口复核状态为 PAID。 |
| 2.6.9 手动记账金额留空 | ✅通过：金额输入框留空提交时，请求 payload 自动使用该账单总金额；记账后接口复核账单状态为 PAID。 |

## 2.7 待确认收款

| 用例 | 结果 |
|---|---|
| 2.7.1 列表与凭证预览 | ✅通过：本批租客的待确认记录正确显示租客名、金额和付款日期；点击凭证缩略图后出现 Vant 图片放大预览。 |
| 2.7.2 确认收款 | ✅通过：确认本批足额上报记录后 toast 显示“已确认”，该租客记录从待确认列表消失；接口复核对应测试账单联动变为 PAID。 |
| 2.7.3 驳回收款 | ✅通过：驳回本批测试记录后 toast 显示“已驳回”，该记录从列表消失；按账单查询收款记录，状态为 REJECTED。 |
| 2.7.4 空状态 | ✅通过：用 Playwright route 将待确认列表响应设为空数组，页面显示“暂无待确认收款”。该场景未处理任何真实待确认记录。 |

## 测试数据清理(Claude Code 执行,2026-08-24)

第一批(2.1-2.3)+第二批(2.4-2.7)测试因 Playwright worker 重启导致重复造数,dev 数据库残留 285 间测试房间(remark 含 `E2E_QA_`)、252 个测试租客、256 份租约、237 张账单、72 条收款记录。清理前先用 `docker exec landlord-easy-mysql mysqldump` 对 `landlordeasy_dev` 打了一份快照(`/home/ubuntu/landlordeasy_dev_pre_e2e_cleanup.sql.gz`,服务器本地)作为安全网。

清理脚本按外键依赖顺序删除:ReminderLog → Payment → BillItem → Bill → DepositRecord → HandoverRecord → Lease → AuditLog(rooms) → Room → Tenant,以 `room.remark` 和 `tenant.name` 含 `E2E_QA_` 为筛选条件,并集出对应 `leaseId`/`billId` 范围。首轮清理后独立核对全库统计发现还有 6 间未打 `E2E_QA_` remark 标记的高位房号房间(888/900-904,是更早的手工功能验证遗留,不是 Kiro 这两批自动化测试产生的)及其关联的 1 条租约,单独补清理;同时发现 1 条 2026-08-23(上次会话 M14 dev/prod 隔离验证)遗留的测试租约(`R栋205`,租客"测试租客dev"),一并清理。

最终核对结果与历史真实数据基线(2026-08-21 记录:185 房间/679 租约/643 押金记录)完全一致:
- 房间:185(清理前191)
- 租约:679(清理前681)
- 押金记录:643(清理前645)
- 账单:清理后 3620 张,比基线 3316 多出的部分经排查是 2026-08-20 的正常历史业务活动(与本次测试无关,已核实创建时间戳),不是清理残留

清理全程未使用 `--force`/`reset` 类破坏性操作,每一步都有独立的 before/after count 核对,未误删任何真实房东历史数据。

## 本批执行信息（2.8–2.11）

- 执行时间：2026-08-24
- 执行命令：`pnpm exec playwright test --config=e2e/dev-remote.config.ts e2e/tests/landlord-maintenance-expense-report.spec.ts`
- 最终统计：**14 passed，0 failed，耗时 47.3s**
- 本批数据前缀：`E2E_QA_1787576980659`

## 2.8 工作台看板明细

| 用例 | 结果 |
|---|---|
| 2.8.1 空置房间页 | ✅通过：接口返回空置房间 `11` 间，页面按 R栋、S栋、明远公寓分组，分组数量之和与总数一致；抽查 R栋 205，接口及页面均显示空置 `3` 天，按 `vacantSince` 重新计算的天数一致。 |
| 2.8.2 到期提醒页 | ✅通过：接口返回 `137` 条；点击首条 R栋 103 的记录后，页面从 `/dashboard/expiring` 跳转到该记录对应的 `/leases/73` 租约详情。该记录页面显示到期日 `2026-01-15`、`-221天后到期`，与当次接口响应一致。 |
| 2.8.3 逾期账单页 | ✅通过：接口返回逾期账单 `292` 张，页面按楼栋分组且分组数量之和与总数一致；抽查 R栋 402 的首条记录，页面显示金额 `¥1500`、逾期 `926` 天，与接口及按 dueDate 重算结果一致。 |
| 2.8.4 点击与空数据表现 | ⚠️现象记录：在真实有数据页面点击空置房间条目和逾期账单条目，URL 均保持不变，没有跳转；分别用 Playwright 将两个看板响应设为 `{ total: 0, buildings: {} }` 后，页面只显示“空置总数 0”或“逾期总数 0”，均没有 `.van-empty` 空状态或“暂无数据”文案。仅记录实测现象，不作结论。 |

## 2.9 维修管理

| 用例 | 结果 |
|---|---|
| 2.9.1 正常新增维修记录 | ✅通过：使用真实房间 ID `301`、日期 `2026-08-24`、内容 `E2E_QA_1787576980659_正常维修`、费用 `91.25` 提交成功；toast 显示“已添加”，列表出现记录，接口复核 ID 为 `1`。 |
| 2.9.2 必填字段留空 | ⚠️现象记录：房间 ID 留空返回 400“房间必须为整数”，未产生记录；日期留空返回 400“日期格式不正确,请使用 YYYY-MM-DD 格式”，未产生记录；内容留空返回 201/success，并产生 1 条内容为空的维修记录（ID `2`、费用 `92.03`）。 |
| 2.9.3 不存在的房间 ID | ⚠️现象记录：房间 ID 填 `999999999` 后提交，响应为 500“服务器内部错误”；随后重新查询维修列表，带本用例内容 `E2E_QA_1787576980659_不存在房间` 的记录数为 `0`，未发现该次提交产生的数据。仅记录实测现象，不作结论。 |
| 2.9.4 房间 ID 输入方式 | ✅通过：该字段实际 DOM 为普通文本输入 `type="text"`，带 `inputmode="decimal"` 以启用数字键盘；字段不是 clickable picker，弹窗内没有选择器。 |

## 2.10 支出管理

| 用例 | 结果 |
|---|---|
| 2.10.1 正常新增支出 | ✅通过：日期、类目、名称、金额和备注正常填写后提交成功；toast 显示“已添加”，列表出现 `E2E_QA_1787576980659_正常支出` 及金额 `¥101.25`，接口复核记录 ID 为 `1322`。 |
| 2.10.2 必填字段留空 | ⚠️现象记录：日期留空返回 400“日期格式不正确,请使用 YYYY-MM-DD 格式”，金额留空返回 400“金额不能小于最小值”，两次均未产生记录；类目留空和名称留空均返回 201/success，各产生 1 条记录（ID `1323`、`1324`），两条备注均带本批 `E2E_QA_` 前缀。 |
| 2.10.3 编辑/删除入口 | ⚠️功能现状：支出列表没有“编辑”或“删除”按钮，也没有滑动操作入口；本用例只确认当前前端展示。 |

## 2.11 报表

| 用例 | 结果 |
|---|---|
| 2.11.1 有数据月份 | ✅通过：查询 `2026-08`，收入概览显示应收 `164380`、实收 `4160`、收缴率 `2.53%`、净收益 `-305.3`；按楼栋、支出（合计 `4465.3` 及分类）、空置（11 间、`5.95%`、损失估算 `32289`）和押金汇总各区块均正常展示，并与当次接口响应逐项一致，未出现 NaN/undefined/null。 |
| 2.11.2 非法月份格式 | ⚠️现象记录：输入 `abc` 后月报接口返回 200/success（响应 month 为 `abc`、应收/实收均为 0），页面继续显示报表且无 toast；输入 `2026-13` 后接口返回 500“服务器内部错误”，页面不白屏、保留报表区块并显示同文案 toast；输入空字符串后接口返回 200/success（响应 month 为空、应收/实收均为 0），页面继续显示报表且无 toast。三种场景均未出现 NaN/undefined/null。仅记录实测现象，不作结论。 |
| 2.11.3 无账单月份 | ⚠️现象记录：查询 `2099-01` 后，应收、实收、收缴率、支出、净收益和空置损失均为 0，按楼栋数据为空；空置仍按当前房间状态显示总房数 `185`、空置 `11`、空置率 `5.95%`，押金汇总仍展示累计值。页面保留收入概览、按楼栋、支出、空置、押金五个区块，没有空状态提示，也未出现 NaN/undefined/null。 |

## 本批执行信息（2.12–3）

- 执行时间：2026-08-24
- 目标环境：`https://dev.landlordeasy.cn`
- 测试账号：`mock_landlord_001`（执行前通过白名单与 JWT 双重确认账号 ID 为 `1`）
- 浏览器：系统 Chrome（Playwright `channel: 'chrome'`）
- 执行命令：`pnpm exec playwright test --config=e2e/dev-remote.config.ts e2e/tests/landlord-settings.spec.ts`
- 最终统计：**23 passed，0 failed，耗时 54.5s**
- 本批数据前缀：`E2E_QA_78169185`

## 2.12 我的

| 用例 | 结果 |
|---|---|
| 2.12.1 4 个入口跳转 | ✅通过：经营报表、维修记录、支出管理、系统设置分别正确跳转到 `/reports`、`/maintenance`、`/expenses`、`/settings`。 |
| 2.12.2 退出登录 | ⚠️体验问题：点击“退出登录”前后均未出现确认弹窗；页面立即跳转 `/login`，`localStorage.landlord_token` 同步清空。仅记录问题D的实测现象。 |
| 2.12.3 ICP 备案 footer | ✅通过：`沪ICP备2026037197号` footer 在 `/mine` 可见；切换到工作台和房间页后均不存在。 |

## 2.13 系统设置

| 用例 | 结果 |
|---|---|
| 2.13.1 提醒参数持久化 | ✅通过：修改提前提醒天数和逾期间隔天数后，PUT 保存成功并显示“已保存”；刷新页面后输入值仍为修改值，GET 接口读回一致。测试后已恢复原值。 |
| 2.13.2 0、负数、非整数边界 | ⚠️现象记录：两个提醒参数同时依次提交 `0`、`-1`、`1.5`，三次均返回 HTTP 200/success，GET 读回值分别为 `0/0`、`-1/-1`、`1.5/1.5`。每轮后均恢复原值；仅记录实际接受情况，不作缺陷结论。 |
| 2.13.3 收款码上传 | ✅通过：为文件输入框选择一张名为 `E2E_QA_78169185_qrcode.png` 的有效 PNG 后，无需额外点击即自动发起 `/admin/qrcode-upload`；接口成功、toast 显示“上传成功”，页面预览图 `src` 与返回 URL 一致。测试后 `qrcodeImageUrl` 已恢复原值。 |
| 2.13.4 管理入口跳转 | ✅通过：白名单管理、楼栋管理、房型模板管理分别正确跳转到 `/settings/landlords`、`/settings/buildings`、`/settings/room-types`。 |

## 2.14 房东白名单

| 用例 | 结果 |
|---|---|
| 2.14.1 新增测试房东 | ✅通过：新增 `E2E_QA_78169185_landlord`（ID `4`），列表出现对应姓名/OpenID，状态 Tag 为“启用”。 |
| 2.14.2 重复 OpenID | ✅通过：再次提交同一 OpenID 返回 HTTP 400，提示“该 openid 已存在”；列表及接口复核仍只有一条该 OpenID 记录。 |
| 2.14.3 禁用测试房东 | ✅通过：禁用 ID `4` 后 toast 显示“已禁用”，Tag 变为“禁用”，该记录的禁用按钮消失。 |
| 2.14.4 禁用当前账号自己 | ✅通过（高风险保护）：操作前确认 `mock_landlord_001` 对应白名单 ID 与当前 JWT `sub` 均为 `1`，并先取得独立测试账号 token 作为紧急恢复通道；实际 DELETE 返回 HTTP 400“不能移除自己”，当前账号始终保持启用，未触发恢复操作。 |
| 2.14.5 禁用后的重新启用入口 | ⚠️功能现状：已禁用的测试房东没有任何按钮，也没有“重新启用/启用账号”入口。 |

## 2.15 楼栋管理

| 用例 | 结果 |
|---|---|
| 2.15.1 新增与房间页联动 | ✅通过：新增 `E2E_QA_78169185_测试楼栋`、排序 901 后，管理列表显示正确；切换房间页后楼栋 Tab 同步出现。 |
| 2.15.2 编辑楼栋 | ✅通过：编辑弹窗正确回填原名称和排序；保存为 `E2E_QA_78169185_测试楼栋_已编辑`、排序 902 后，列表及 GET 接口均持久化新值。 |
| 2.15.3 删除有房间的真实楼栋 | ✅通过（高风险保护）：点击前动态确认目标为 Q栋（ID `5`），且关联 **10 间**非测试真实房间；只通过页面正常删除入口提交一次，后端返回 HTTP 400“该楼栋下有房间,无法删除”。随后接口复核 Q栋及关联房间均仍存在，未调用任何绕过接口、未发生删除。 |
| 2.15.4 删除测试楼栋 | ✅通过：删除前确认测试楼栋关联房间数为 0；通过页面确认框删除成功，toast 显示“已删除”，列表和 GET 接口均不再存在该楼栋。 |

## 2.16 房型管理

| 用例 | 结果 |
|---|---|
| 2.16.1 新增测试房型 | ✅通过：新增 `E2E_QA_78169185_测试房型`，默认租金 1888、押金 2888、付款周期季付；toast、列表及接口返回均正确。 |
| 2.16.2 编辑房型 | ✅通过：编辑弹窗正确回填名称、租金 1888、押金 2888 和季付；改为 `E2E_QA_78169185_测试房型_已编辑`、租金 1999、押金 2999、年付后，列表和 GET 接口均持久化新值。 |
| 2.16.3 删除正被使用的真实房型 | ✅通过（高风险保护）：点击前动态确认目标为“Q栋通用房型”（ID `4`），且正被 **10 间**非测试真实房间使用；只通过页面正常删除入口提交一次，后端返回 HTTP 400“有房间使用该房型,无法删除”。随后接口复核该房型及引用房间均仍存在，未调用任何绕过接口、未发生删除。 |
| 2.16.4 删除测试房型 | ✅通过：删除前确认测试房型引用房间数为 0；通过页面确认框删除成功，toast 显示“已删除”，列表和 GET 接口均不再存在该房型。 |

## 3 跨模块链路测试

| 用例 | 结果 |
|---|---|
| 3.1 完整业务闭环 | ✅通过：在本批自建空置房间 Q栋 `8169185`（房间 ID `790`）完成 UI 新签，房间由 VACANT 变 RENTED；调用真实 `/bills/generate` 生成账单 ID `4132`；UI 全额现金记账 1777 后账单变 PAID；房间详情“当前欠费”消失；UI 退租后租约 ID `968` 变 ENDED，房间重新变 VACANT，详情仍无当前欠费。每一步均通过页面与 API 双重核对。 |
| 3.2 楼栋管理联动 | ✅通过：在楼栋管理页新增 `E2E_QA_78169185_联动楼栋` 后，仅通过 SPA 导航切换到房间页及批量建房页，未刷新整个应用；楼栋选择器立即出现新楼栋。 |
| 3.3 房型联动 | ✅通过：新增 `E2E_QA_78169185_联动房型` 后，在批量建房页同时选中新楼栋和新房型创建 2 间房；接口确认两间房的 `roomTypeId` 正确，进入其中一间详情后“房型”显示新房型名称。验证后两间无租约房通过现有 DELETE API 删除，联动房型和楼栋均通过页面删除入口清理。 |

### 本批测试数据与清理

- 系统设置已只读复核恢复为提前提醒 `3`、逾期间隔 `3`、收款码 URL 为空；当前账号 `mock_landlord_001` 保持启用。上传测试在服务器生成过一张 `qrcode_*.png` 文件，产品没有附件删除接口；设置引用已恢复，不再指向该文件。
- 2.15 模块测试楼栋、2.16 模块测试房型均已通过 UI 删除；3.2/3.3 的联动楼栋、联动房型也已通过 UI 删除，两间联动房在无租约时通过现有房间 DELETE API 删除。最终只读核对：`E2E_QA_` 楼栋 **0**、房型 **0**。
- 白名单记录 ID `4`（`E2E_QA_78169185_landlord`）按产品软删除逻辑保留为 `isActive=false`；页面及后端均没有彻底删除入口。该账号不能登录，也没有重新启用入口。
- 3.1 因产品要求保留租约历史且有关联租约的房间禁止删除，保留最小闭环数据：房间 ID `790`（Q栋 `8169185`，VACANT）、测试租客 ID `964`、已结束租约 ID `968`、已付账单 ID `4132`、已确认现金收款 ID `3449`，以及对应押金/审计记录。均可通过前缀 `E2E_QA_78169185` 或上述 ID 精确识别；未修改任何真实历史租约/账单。
- 本批没有使用 Prisma、直接数据库操作或临时清理脚本；除上述产品模型要求保留的软删除/历史链路数据及上传文件外，其余可删除测试资源已在 Playwright 脚本内完成清理。
